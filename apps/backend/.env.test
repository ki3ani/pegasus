# Test Database - Uses the same Docker container but different database
DATABASE_URL="postgresql://rebalancex_user:secure_password_123@localhost:5433/rebalancex_test?schema=public"

# Test JWT
JWT_SECRET="test-jwt-secret-key-for-testing-only"
JWT_EXPIRES_IN="1h"

# Stellar Network
STELLAR_NETWORK="testnet"
STELLAR_HORIZON_URL="https://horizon-testnet.stellar.org"

# Server
PORT=3001
FRONTEND_URL="http://localhost:3000"

# Encryption
WALLET_ENCRYPTION_KEY="test-wallet-encryption-key-32chars"